fastapi>=0.110
uvicorn[standard]>=0.27
openai>=1.12
pydantic>=2.5
python-dotenv>=1.0
httpx>=0.26
pyyaml>=6.0
pytest>=8.0

# Optional production backends (only needed when DATABASE_URL / REDIS_URL are set).
# Install with:  pip install "psycopg[binary]>=3.1" "redis>=5.0"
# psycopg[binary]>=3.1
# redis>=5.0
